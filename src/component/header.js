

import React, { Component } from "react";
import { Header, Left, Button, Title, Body, Right } from "react-navigation";
export default class FTR extends Component {
    render() {
        return (
            
        );
    }
}