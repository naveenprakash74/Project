import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: "#FFF"
    },
    mb: {
        marginTop: 100
    },
    success: {
        color: "#008000"
    },
    error: {
        color: "#FF0000"
    },
});
